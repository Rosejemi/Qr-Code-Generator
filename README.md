# Qr-Code-Generator
A Node.js command-line application that generates a QR code from a user-provided URL. It uses npm packages to collect input, create a QR image, and save the entered URL to a text file. This project demonstrates npm usage, file handling, and ES module concepts in Node.js.
